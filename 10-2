#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int intpow(int m, int n);

int main() {
	
	int x, y;

	printf("두 개의 정수값을 입력하시오 : ");
	scanf("%d %d", &x, &y, sizeof(int));
	
	printf("결과는 %d입니다.\n", intpow(x,y));

	return 0;
}

int intpow(int m, int n)
{
	int xy = 0;
	xy = m * n;

	return xy;
}
